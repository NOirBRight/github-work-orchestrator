interface:
  display_name: "Orchestrator"
  short_description: "Preflight and run parallel rolling GitHub work"
  default_prompt: "Use $orchestrator to scan the repository frontier, admit decision-complete Issues, and dispatch the next safe parallel wave."

policy:
  allow_implicit_invocation: true
