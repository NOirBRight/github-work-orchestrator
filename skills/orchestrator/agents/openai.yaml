interface:
  display_name: "Orchestrator (compatibility alias)"
  short_description: "Temporary alias for Implement with GWO"
  default_prompt: "Use $implement-gwo for this request and note that $orchestrator is removed in V8.1."

policy:
  allow_implicit_invocation: false
