interface:
  display_name: "Orchestrator"
  short_description: "Plan and run lightweight rolling GitHub work waves"
  default_prompt: "Use $orchestrator to triage this repository and dispatch the next safe rolling wave."

policy:
  allow_implicit_invocation: true
