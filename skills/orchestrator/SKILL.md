---
name: orchestrator
description: "One-release compatibility alias for the durable GWO V8 /implement-gwo workflow; scheduled for removal in V8.1."
---

# Orchestrator compatibility alias

This is a compatibility alias only. Emit a concise notice that `$orchestrator`
is removed in V8.1, then delegate the unchanged user request to
`$implement-gwo`.

Do not scan or plan independently, add lifecycle behavior, invoke Matt
`/implement`, or create a second orchestration workflow.
