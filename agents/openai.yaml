interface:
  display_name: "GitHub Work Orchestrator"
  short_description: "Dispatch GitHub issues into safe parallel Codex tasks"
  default_prompt: "Use $github-work-orchestrator to compute the ready frontier and safely dispatch visible Codex tasks."
