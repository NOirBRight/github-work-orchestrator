interface:
  display_name: "GitHub Work Orchestrator"
  short_description: "Align direction, reconcile Issues, and dispatch Codex tasks"
  default_prompt: "Use $github-work-orchestrator to align project direction, surface architecture decisions, reconcile Issue state, and dispatch safe parallel Codex tasks."
